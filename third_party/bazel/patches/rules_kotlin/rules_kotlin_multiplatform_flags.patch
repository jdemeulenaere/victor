--- a/src/main/starlark/core/options/opts.kotlinc.bzl
+++ b/src/main/starlark/core/options/opts.kotlinc.bzl
@@ -35,6 +35,11 @@
 
 def _map_optin_class_to_flag(values):
     return ["-opt-in=%s" % v for v in values]
+
+def _map_prefixed_flag_list_to_flags(prefix, values):
+    if not values:
+        return None
+    return ["{}{}".format(prefix, value) for value in values]
 
 def _map_backend_threads_to_flag(n):
     if n == 1:
@@ -325,8 +330,71 @@
         value_to_flag = {
             "class": ["-Xlambdas=class"],
             "indy": ["-Xlambdas=indy"],
+        },
+    ),
+    "x_common_sources": struct(
+        flag = "-Xcommon-sources",
+        args = dict(
+            default = [],
+            doc = """Sources of the common module that should compile together with this platform module in multiplatform mode.
+Each value must match a source path passed to this compilation.""",
+        ),
+        type = attr.string_list,
+        value_to_flag = None,
+        map_value_to_flag = lambda values: _map_prefixed_flag_list_to_flags("-Xcommon-sources=", values),
+    ),
+    "x_expect_actual_classes": struct(
+        flag = "-Xexpect-actual-classes",
+        args = dict(
+            default = False,
+            doc = "Enable beta support for expect/actual classes without compiler warning.",
+        ),
+        type = attr.bool,
+        value_to_flag = {
+            True: ["-Xexpect-actual-classes"],
         },
     ),
+    "x_fragment_sources": struct(
+        flag = "-Xfragment-sources",
+        args = dict(
+            default = [],
+            doc = "Add sources to a specific fragment using <fragment>:<path> entries.",
+        ),
+        type = attr.string_list,
+        value_to_flag = None,
+        map_value_to_flag = lambda values: _map_prefixed_flag_list_to_flags("-Xfragment-sources=", values),
+    ),
+    "x_fragments": struct(
+        flag = "-Xfragments",
+        args = dict(
+            default = [],
+            doc = "Declare known fragments for separate KMP compilation.",
+        ),
+        type = attr.string_list,
+        value_to_flag = None,
+        map_value_to_flag = lambda values: _map_prefixed_flag_list_to_flags("-Xfragments=", values),
+    ),
+    "x_fragment_refines": struct(
+        flag = "-Xfragment-refines",
+        args = dict(
+            default = [],
+            doc = "Declare <fromFragment>:<toFragment> refine relations for separate KMP compilation.",
+        ),
+        type = attr.string_list,
+        value_to_flag = None,
+        map_value_to_flag = lambda values: _map_prefixed_flag_list_to_flags("-Xfragment-refines=", values),
+    ),
+    "x_metadata_klib": struct(
+        flag = "-Xmetadata-klib",
+        args = dict(
+            default = False,
+            doc = "Produce a klib that only contains declaration metadata.",
+        ),
+        type = attr.bool,
+        value_to_flag = {
+            True: ["-Xmetadata-klib"],
+        },
+    ),
     "x_multi_platform": struct(
         flag = "-Xmulti-platform",
         args = dict(
@@ -336,8 +404,41 @@
         type = attr.bool,
         value_to_flag = {
             True: ["-Xmulti-platform"],
+        },
+    ),
+    "x_separate_kmp_compilation": struct(
+        flag = "-Xseparate-kmp-compilation",
+        args = dict(
+            default = False,
+            doc = "Enable the separated multiplatform compilation scheme.",
+        ),
+        type = attr.bool,
+        value_to_flag = {
+            True: ["-Xseparate-kmp-compilation"],
         },
     ),
+    "x_no_check_actual": struct(
+        flag = "-Xno-check-actual",
+        args = dict(
+            default = False,
+            doc = "Do not check for presence of actual declarations in this compilation.",
+        ),
+        type = attr.bool,
+        value_to_flag = {
+            True: ["-Xno-check-actual"],
+        },
+    ),
+    "xx_lenient_mode": struct(
+        flag = "-XXlenient-mode",
+        args = dict(
+            default = False,
+            doc = "Enable lenient multiplatform mode for missing actual declarations.",
+        ),
+        type = attr.bool,
+        value_to_flag = {
+            True: ["-XXlenient-mode"],
+        },
+    ),
     "x_no_call_assertions": struct(
         flag = "-Xno-call-assertions",
         args = dict(
