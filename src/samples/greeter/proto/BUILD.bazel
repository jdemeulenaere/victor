load("//build/rules:defs.bzl", "grpc_proto")

grpc_proto(
    name = "victor_api",
    srcs = ["greeter.proto"],
    platforms = [
        "jvm",
        "python",
        "web",
    ],
    visibility = ["//visibility:public"],
)
