load("//build/rules:defs.bzl", "grpc_proto")

grpc_proto(
    name = "victor_api",
    srcs = ["greeter.proto"],
    legacy_py_import_prefix = "example/greeter/proto",
    visibility = ["//visibility:public"],
)
